@model Bookstore.Domain.Book
    <div class="well">
            @if(Model.ImageData != null)
            {
                <div class="pull-left" style="margin-right:10px">
                    <img class="img-rounded" width="100" height="100"src="@Url.Action("GetImage", "Book", new { Model.BookID })" />
                    <h3><span class="pull-right label label-primary">@Model.Price.ToString("c")</span></h3>
                </div>
            }
            <h3>ISBN: @Model.BookID</h3>
        <h3>
            @Model.Title
        </h3>
    
            <span class="lead">@Model.Description</span>
        @using (Html.BeginForm("AddToCart", "Cart"))
        {
            <div class="pull-right">
                @Html.HiddenFor(b => b.BookID)
                @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
                <input type="submit" class="btn btn-success" value="Add to cart" />
            </div>
        }
    </div>

